HOME=/usr/src/world
#ROOT=/
ROOT=/owl

RPMS=$HOME/RPMS

FLAGS='--replacepkgs --oldpackage --replacefiles -U'
UMASK=077

# SILO command-line options to use when the package is upgraded (SPARC only)
export SILO_INSTALL="yes"
export SILO_FLAGS=""
