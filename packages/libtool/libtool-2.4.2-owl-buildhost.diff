diff -puNr libtool-2.4.2.orig/configure libtool-2.4.2/configure
--- libtool-2.4.2.orig/configure	2011-10-17 10:18:58 +0000
+++ libtool-2.4.2/configure	2014-06-15 11:25:09 +0000
@@ -557,7 +557,7 @@ exec 6>&1
 # Name of the host.
 # hostname on some systems (SVR3.2, old GNU/Linux) returns a bogus exit status,
 # so uname gets run too.
-ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`
+ac_hostname="localhost"
 
 #
 # Initializations.
@@ -2219,7 +2219,7 @@ cat <<_ASUNAME
 ## Platform. ##
 ## --------- ##
 
-hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
+hostname = localhost
 uname -m = `(uname -m) 2>/dev/null || echo unknown`
 uname -r = `(uname -r) 2>/dev/null || echo unknown`
 uname -s = `(uname -s) 2>/dev/null || echo unknown`
@@ -3260,7 +3260,7 @@ $as_echo "$as_me: autobuild project... $
   { $as_echo "$as_me:${as_lineno-$LINENO}: autobuild revision... $AB_VERSION" >&5
 $as_echo "$as_me: autobuild revision... $AB_VERSION" >&6;}
 
-  hostname=`hostname`
+  hostname=localhost
   if test "$hostname"; then
     { $as_echo "$as_me:${as_lineno-$LINENO}: autobuild hostname... $hostname" >&5
 $as_echo "$as_me: autobuild hostname... $hostname" >&6;}
@@ -27766,7 +27766,6 @@ generated by GNU Autoconf 2.68.  Invocat
   CONFIG_COMMANDS = $CONFIG_COMMANDS
   $ $0 $@
 
-on `(hostname || uname -n) 2>/dev/null | sed 1q`
 "
 
 _ACEOF
@@ -29497,7 +29496,7 @@ $as_echo X"$file" |
 
 # `$ECHO "$ofile" | sed 's%^.*/%%'` - Provide generalized library-building support services.
 # Generated automatically by $as_me ($PACKAGE$TIMESTAMP) $VERSION
-# Libtool was configured on host `(hostname || uname -n) 2>/dev/null | sed 1q`:
+# Libtool was configured as follows:
 # NOTE: Changes made to this file will be lost: look at ltmain.sh.
 #
 #   Copyright (C) 1996, 1997, 1998, 1999, 2000, 2001, 2003, 2004, 2005,
diff -puNr libtool-2.4.2.orig/libltdl/configure libtool-2.4.2/libltdl/configure
--- libtool-2.4.2.orig/libltdl/configure	2011-10-17 10:19:47 +0000
+++ libtool-2.4.2/libltdl/configure	2014-06-15 11:25:52 +0000
@@ -553,7 +553,7 @@ exec 6>&1
 # Name of the host.
 # hostname on some systems (SVR3.2, old GNU/Linux) returns a bogus exit status,
 # so uname gets run too.
-ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`
+ac_hostname=localhost
 
 #
 # Initializations.
@@ -1865,7 +1865,7 @@ cat <<_ASUNAME
 ## Platform. ##
 ## --------- ##
 
-hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
+hostname = localhost
 uname -m = `(uname -m) 2>/dev/null || echo unknown`
 uname -r = `(uname -r) 2>/dev/null || echo unknown`
 uname -s = `(uname -s) 2>/dev/null || echo unknown`
@@ -13047,7 +13047,6 @@ generated by GNU Autoconf 2.68.  Invocat
   CONFIG_COMMANDS = $CONFIG_COMMANDS
   $ $0 $@
 
-on `(hostname || uname -n) 2>/dev/null | sed 1q`
 "
 
 _ACEOF
@@ -14223,7 +14222,7 @@ $as_echo X"$file" |
 
 # `$ECHO "$ofile" | sed 's%^.*/%%'` - Provide generalized library-building support services.
 # Generated automatically by $as_me ($PACKAGE$TIMESTAMP) $VERSION
-# Libtool was configured on host `(hostname || uname -n) 2>/dev/null | sed 1q`:
+# Libtool was configured as follows:
 # NOTE: Changes made to this file will be lost: look at ltmain.sh.
 #
 #   Copyright (C) 1996, 1997, 1998, 1999, 2000, 2001, 2003, 2004, 2005,
diff -puNr libtool-2.4.2.orig/libltdl/m4/autobuild.m4 libtool-2.4.2/libltdl/m4/autobuild.m4
--- libtool-2.4.2.orig/libltdl/m4/autobuild.m4	2011-10-09 21:45:18 +0000
+++ libtool-2.4.2/libltdl/m4/autobuild.m4	2014-06-15 11:26:27 +0000
@@ -23,7 +23,7 @@ AC_DEFUN([AB_INIT],
   fi
   AC_MSG_NOTICE([autobuild revision... $AB_VERSION])
 
-  hostname=`hostname`
+  hostname=localhost
   if test "$hostname"; then
     AC_MSG_NOTICE([autobuild hostname... $hostname])
   fi
diff -puNr libtool-2.4.2.orig/libltdl/m4/libtool.m4 libtool-2.4.2/libltdl/m4/libtool.m4
--- libtool-2.4.2.orig/libltdl/m4/libtool.m4	2011-10-17 10:17:05 +0000
+++ libtool-2.4.2/libltdl/m4/libtool.m4	2014-06-15 11:26:09 +0000
@@ -720,7 +720,7 @@ _LT_CONFIG_SAVE_COMMANDS([
 
 # `$ECHO "$ofile" | sed 's%^.*/%%'` - Provide generalized library-building support services.
 # Generated automatically by $as_me ($PACKAGE$TIMESTAMP) $VERSION
-# Libtool was configured on host `(hostname || uname -n) 2>/dev/null | sed 1q`:
+# Libtool was configured as follows:
 # NOTE: Changes made to this file will be lost: look at ltmain.sh.
 #
 _LT_COPYING
