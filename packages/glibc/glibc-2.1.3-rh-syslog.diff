diff -urN glibc-2.1.3.orig/misc/syslog.c glibc-2.1.3.rh/misc/syslog.c
--- glibc-2.1.3.orig/misc/syslog.c	Sat Feb 12 01:49:57 2000
+++ glibc-2.1.3.rh/misc/syslog.c	Wed Feb  2 00:15:08 2000
@@ -281,6 +281,13 @@
 					__set_errno (old_errno);
 					continue;
 				}
+				if (LogType == SOCK_STREAM
+				    && saved_errno == ECONNREFUSED) {
+				    /* retry with next SOCK_DGRAM: */
+				    LogType = SOCK_DGRAM;
+				    __set_errno (old_errno);
+				    continue;
+				}
 			} else
 				connected = 1;
 		}
