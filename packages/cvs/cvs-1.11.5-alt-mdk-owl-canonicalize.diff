diff -ur cvs-1.11.5.orig/src/root.c cvs-1.11.5/src/root.c
--- cvs-1.11.5.orig/src/root.c	Fri Dec  6 19:10:28 2002
+++ cvs-1.11.5/src/root.c	Wed Apr 30 23:54:18 2003
@@ -10,6 +10,8 @@
  * Determine the path to the CVSROOT and set "Root" accordingly.
  */
 
+#define _GNU_SOURCE
+
 #include "cvs.h"
 #include "getline.h"
 
@@ -571,6 +573,14 @@
 	    error (0, 0, "when using local access method.");
 	    goto error_exit;
 	}
+	{
+	    char *dir = canonicalize_file_name (newroot->directory);
+	    if (dir)
+	    {
+		free (newroot->directory);
+		newroot->directory = dir;
+	    }
+	}
 	no_port = 1;
 	no_password = 1;
 	break;
@@ -724,7 +734,7 @@
 
     newroot->original = xstrdup(dir);
     newroot->method = local_method;
-    newroot->directory = xstrdup(dir);
+    newroot->directory = canonicalize_file_name (dir) ?: xstrdup(dir);
 
     return newroot;
 }
diff -ur cvs-1.11.5.orig/src/server.c cvs-1.11.5/src/server.c
--- cvs-1.11.5.orig/src/server.c	Thu Jan 16 15:10:55 2003
+++ cvs-1.11.5/src/server.c	Wed Apr 30 23:54:28 2003
@@ -8,6 +8,8 @@
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.  */
 
+#define _GNU_SOURCE
+
 #include <assert.h>
 #include "cvs.h"
 #include "watch.h"
@@ -1151,6 +1153,14 @@
     status = buf_read_line (buf_from_net, &repos, (int *) NULL);
     if (status == 0)
     {
+	char *real_repos = canonicalize_file_name (repos);
+
+	if (real_repos)
+	{
+	    free (repos);
+	    repos = real_repos;
+	}
+
 	if (!outside_root (repos))
 	    dirswitch (arg, repos);
 	free (repos);
