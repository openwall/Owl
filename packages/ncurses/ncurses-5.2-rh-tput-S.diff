--- ncurses-5.2/progs/tput.c.org	Sun Apr 15 03:00:34 2001
+++ ncurses-5.2/progs/tput.c	Mon Jun 18 16:04:49 2001
@@ -132,7 +132,9 @@
     int status;
     FILE *f;
 
-    check_aliases(name = argv[0]);
+    if (!(name = argv[0]))
+	name = "<none>";
+    check_aliases(name);
     if (is_reset || is_init) {
 	if (init_prog != 0) {
 	    system(init_prog);
