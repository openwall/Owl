--- rpm-3.0.6/autodeps/linux.req.orig	Wed Jun 14 16:34:28 2000
+++ rpm-3.0.6/autodeps/linux.req	Fri Jan 17 02:46:56 2003
@@ -34,7 +34,7 @@
 	    sub(/:/, "", $3);
 	    LIBNAME=$3;
 	}
-	(START==1) && (LIBNAME!="") && ($4~/^GLIBC_*/) { print LIBNAME "(" $4 ")"; }
+	(START==1) && (LIBNAME!="") && ($4!="") { print LIBNAME "(" $4 ")"; }
 	/^$/ { START=0; }
     '
 done | sort -u
