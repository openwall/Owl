diff -puNr rpm-4.2.orig/lib/psm.c rpm-4.2/lib/psm.c
--- rpm-4.2.orig/lib/psm.c	Wed Dec  8 00:06:12 2004
+++ rpm-4.2/lib/psm.c	Wed Dec  8 00:15:08 2004
@@ -1693,6 +1693,8 @@ psm->te->h = headerLink(fi->h);
 	}
 	break;
     case PSM_PROCESS:
+	rc = rpmpsmStage(psm, PSM_CHROOT_IN);
+
 	if (rpmtsFlags(ts) & RPMTRANS_FLAG_TEST)	break;
 
 	if (psm->goal == PSM_PKGINSTALL) {
