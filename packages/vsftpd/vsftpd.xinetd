# default: off
# description: An FTP server

service ftp
{
	socket_type	= stream
	protocol	= tcp
	user		= root
	wait		= no
	rlimit_as	= 16M
	server		= /usr/sbin/vsftpd
	server_args	= -o listen=NO
	disable         = yes
}
