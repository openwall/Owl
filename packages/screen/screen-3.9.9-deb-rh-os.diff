--- screen-3.9.9/os.h.orig	Sat Apr 28 16:26:43 2001
+++ screen-3.9.9/os.h	Tue Jun 12 17:40:27 2001
@@ -77,6 +77,9 @@
 #  undef strlen
 # else /* NEWSOS */
 #  include <strings.h>
+#  if defined(__GLIBC__) && (__GLIBC__ >= 2)
+#   include <string.h>
+#  endif
 # endif /* NEWSOS */
 #else /* SYSV */
 # if defined(SVR4) || defined(NEWSOS)
@@ -266,7 +269,7 @@
 #  define pututline	pututxline
 #  define setutent	setutxent
 #  define endutent	endutxent
-#  define ut_time	ut_xtime
+#  define ut_time	ut_tv.tv_sec
 # else /* SVR4 */
 #  include <utmp.h>
 # endif /* SVR4 */
@@ -287,7 +290,7 @@
 #   ifdef _PATH_UTMP
 #    define UTMPFILE	_PATH_UTMP
 #   else
-#    define UTMPFILE	"/etc/utmp"
+#    define UTMPFILE	"/var/run/utmp"
 #   endif /* _PATH_UTMP */
 #  endif
 # endif
--- screen-3.9.9/osdef.h.in.orig	Sat Apr 28 16:26:43 2001
+++ screen-3.9.9/osdef.h.in	Tue Jun 12 17:40:27 2001
@@ -96,7 +96,6 @@
 extern int   setegid __P((int));
 #endif
 
-extern char *crypt __P((char *, char *));
 extern int   putenv __P((char *));
 
 extern int   tgetent __P((char *, char *));
