diff -ur openssh-3.0.2p1.orig/scp.c openssh-3.0.2p1/scp.c
--- openssh-3.0.2p1.orig/scp.c	Mon Oct 22 04:53:59 2001
+++ openssh-3.0.2p1/scp.c	Wed Dec 12 19:36:19 2001
@@ -90,7 +90,7 @@
 #endif
 
 /* For progressmeter() -- number of seconds before xfer considered "stalled" */
-#define STALLTIME	5
+#define STALLTIME	60
 /* alarm() interval for updating progress meter */
 #define PROGRESSTIME	1
 
@@ -1159,10 +1159,12 @@
 	if (cursize > lastsize) {
 		lastupdate = now;
 		lastsize = cursize;
+#if 0
 		if (wait.tv_sec >= STALLTIME) {
 			start.tv_sec += wait.tv_sec;
 			start.tv_usec += wait.tv_usec;
 		}
+#endif
 		wait.tv_sec = 0;
 	}
 	timersub(&now, &start, &td);
