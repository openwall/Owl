diff -ur openssh-2.5.1p1.orig/scp.c openssh-2.5.1p1/scp.c
--- openssh-2.5.1p1.orig/scp.c	Mon Feb 19 13:51:08 2001
+++ openssh-2.5.1p1/scp.c	Sun Mar 18 04:12:48 2001
@@ -90,7 +90,7 @@
 #endif
 
 /* For progressmeter() -- number of seconds before xfer considered "stalled" */
-#define STALLTIME	5
+#define STALLTIME	60
 
 /* Progress meter bar */
 #define BAR \
@@ -1184,10 +1184,12 @@
 	if (cursize > lastsize) {
 		lastupdate = now;
 		lastsize = cursize;
+#if 0
 		if (wait.tv_sec >= STALLTIME) {
 			start.tv_sec += wait.tv_sec;
 			start.tv_usec += wait.tv_usec;
 		}
+#endif
 		wait.tv_sec = 0;
 	}
 	timersub(&now, &start, &td);
