--- tar-1.13.19/src/incremen.c.orig	Sat Jan 13 08:59:29 2001
+++ tar-1.13.19/src/incremen.c	Tue Jul 10 15:40:03 2001
@@ -182,8 +182,11 @@
     struct directory *directory; /* for checking if already already seen */
     enum children children;
 
-    if (! dirp)
+    if (! dirp) {
+      /* Assume errno is the reason why */
       savedir_error (path);
+      return NULL;
+    }
     errno = 0;
 
     name_buffer_size = strlen (path) + NAME_FIELD_SIZE;
