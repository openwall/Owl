--- ltrace-0.3.10/process_event.c.nsyscals0	Thu Aug  3 15:12:28 2000
+++ ltrace-0.3.10/process_event.c	Thu Aug  3 15:12:41 2000
@@ -86,7 +86,7 @@
 	};
 	int nsyscals0 = sizeof syscalent0 / sizeof syscalent0[0];
 
-	if (sysnum<0 || sysnum>nsyscals0) {
+	if (sysnum<0 || sysnum>=nsyscals0) {
 		sprintf(result, "SYS_%d", sysnum);
 		return result;
 	} else {
