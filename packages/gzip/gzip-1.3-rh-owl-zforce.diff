diff -ur gzip-1.3.orig/zforce.in gzip-1.3/zforce.in
--- gzip-1.3.orig/zforce.in	Fri Jun 11 15:36:14 1993
+++ gzip-1.3/zforce.in	Sun Jul  7 03:25:58 2002
@@ -25,8 +25,7 @@
   test `expr "$i" : '.*[.-]gz$'` -eq 0 || continue
   test `expr "$i" : '.*[.]t[ag]z$'` -eq 0 || continue
 
-  if gzip -l < "$i" 2>/dev/null | grep '^defl' > /dev/null; then
-
+  if gzip -l < "$i" 2>/dev/null | grep -q '^compressed'; then
     if test `expr "$i" : '^............'` -eq 12; then
       new=`expr "$i" : '\(.*\)...$`.gz
     else
